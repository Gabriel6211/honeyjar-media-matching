services:
  db:
    image: pgvector/pgvector:pg17
    container_name: honeyjar-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: honeyjar
      POSTGRES_PASSWORD: honeyjar
      POSTGRES_DB: honeyjar
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
